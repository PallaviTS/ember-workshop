<div class="container">
  
  <form>
    
    <div class="form-group">
      <label for="owner">Owner</label>
      {{input name="owner" type="text" value=ownerName}}
    </div>

    <div class="form-group">
      <label for="to">to</label>
      {{input name="to" type="text" value=to}}
    </div>

    <div class="form-group">
      <label for="from">from</label>
      {{input name="from" type="text" value=from}}
    </div>

    <div class="form-group">
      <label for="contactno">contact no</label>
      {{input name="contactno" type="text" value=contactNo}}
    </div>

  <button {{action 'savePost'}}>Save this post</button>

  </form>

  <span>{{ownerName}}</span>

</div>